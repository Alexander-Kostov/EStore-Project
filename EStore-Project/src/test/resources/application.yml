spring:
  sql:
    init:
      mode: never
  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
    open-in-view: false
app:
  admin:
    defaultpass: ${DEFAULT_PASS:test}